# Project Caligo landing page: post-fix technical audit

Re-audited `index.html`, `styles.css`, and `script.js` against the rendered page at `http://127.0.0.1:4173/` after the Impeccable live pass on 2026-07-16.

## Audit health score

| # | Dimension | Score | Key finding |
|---|-----------|-------|-------------|
| 1 | Accessibility | 4/4 | AA contrast, semantic structure, validation, focus, target sizing, reduced motion, and current-location feedback are present. |
| 2 | Performance | 3/4 | Lean vanilla implementation with stable geometry; the two photographs remain JPEG-only. |
| 3 | Responsive Design | 3/4 | Fluid grids, corrected hero sizing, 2 by 2 phone outcomes, deliberate mobile footer, and fixed-header anchor offsets. |
| 4 | Theming | 4/4 | All component colors and alpha surfaces now resolve through semantic root tokens. |
| 5 | Anti-Patterns | 4/4 | The detector returns no findings; numbered scaffolding and arbitrary decorative glyphs are gone. |
| **Total** | | **18/20** | **Excellent, minor polish only** |

## Anti-pattern verdict

**Pass.** The landing page now reads as a specific Project Caligo product surface rather than a generic dark-tech template. The imagery, serif wordmark, precise sans-serif hierarchy, mono operational labels, periwinkle refraction, sharp rules, and outcome-led copy form one coherent system. The current detector reports zero anti-patterns.

## Executive summary

- Audit Health Score: **18/20 (Excellent)**, up from **12/20**.
- Remaining issues: **0 P0 / 0 P1 / 1 P2 / 1 P3**.
- Fixed: hero geometry, Caligo mark geometry, hash offsets, active navigation, target sizing, decorative inconsistency, process-card excess, mobile outcome/footer composition, and token drift.
- Remaining work is optional asset optimization and device-matrix visual QA.

## Remaining findings

### [P2] Photographs have no next-generation source

- **Location:** `assets/mirage-worlds.jpg`, `assets/caligo-wing.jpg`; `index.html:62-68,126-133`
- **Category:** Performance
- **Impact:** The 239KB hero and 380KB lower image are reasonable but larger than a tuned AVIF/WebP picture source would be, especially on mobile networks.
- **Recommendation:** Generate visually verified AVIF/WebP variants and use `<picture>` with the JPEGs as fallbacks.
- **Suggested command:** `$impeccable optimize`

### [P3] Full physical-device matrix remains to be sampled

- **Location:** Responsive CSS at `styles.css:1285-1620`
- **Category:** Responsive
- **Impact:** Desktop live inspection and breakpoint/code verification pass, but a final visual sample on physical Safari/iOS and Chrome/Android would catch platform font-metric differences.
- **Recommendation:** Sample 390px, 768px, 1024px, and wide desktop in the deployment preview before a paid campaign.
- **Suggested command:** `$impeccable adapt`

## Positive findings

- The hero now renders at 720px with a 550 by 344 landscape visual rather than stretching to 1,256px.
- Both Caligo wings render at 221px instead of collapsing to 2px.
- Direct hash navigation clears the fixed header and the navigation reflects the current section, including the contact CTA.
- Form validation announces distinct malformed-email feedback and correctly sets `aria-invalid`.
- There is no horizontal overflow at the inspected 1280px viewport, and responsive rules explicitly recompose major grids at 1000px, 820px, and 600px.
- All colors below `:root` reference tokens; the remaining raw color values are definitions inside the token layer.
- The Impeccable anti-pattern detector returns `[]`.

## Recommended actions

1. **[P2] `$impeccable optimize`**: Add visually verified AVIF/WebP picture sources when an image encoder is available.
2. **[P3] `$impeccable adapt`**: Sample the deployed build on representative physical devices.
3. **[P3] `$impeccable polish`**: Make only evidence-backed optical adjustments after device sampling.

Re-run `$impeccable audit` after any asset or layout change to preserve the score.
